請撰寫一個程式，讀入一串未知長度的字串，並輸出其中所包含的數字，且必須按照原先順序。例如輸入「abc123asd4056qwe789」則需要輸出「1234056789」。

while True:  
    y = list(input().split(','))  
    for i in y:  
        str1 = i  
        num = ''.join([x for x in str1 if x.isdigit()])  
        print(num) 
