請撰寫一個程式，讀入第一行t代表後續有t行，後續每行皆有n組邊長，每組有3個邊長a, b, c(因此每行共有3n個邊長)，每行間的n不一定相同。請判斷每組邊長a, b, c是否可以構成三角形，並且每行都需要輸出「可以構成三角形的組數,不可構成三角形的組數」，例如某行可以構成三角形的組數共有3組，不可構成三角形的組數有8組，則輸出「3,8」。

※ 三邊長若要構成三角形，需要「任兩邊長相加 > 第三邊長」
讀入第一行t代表後續有t行，後續每行皆有n組邊長，每組有3個邊長a, b, c(因此每行共有3n個邊長)，每行間的n不一定相同。
請判斷每組邊長a, b, c是否可以構成三角形，並且每行都需要輸出「可以構成三角形的組數,不可構成三角形的組數」，例如某行可以構成三角形的組數共有3組，不可構成三角形的組數有8組，則輸出「3,8」。

t = int(input())  
for i in range(t):  
    length = list(map(int, input().split(",")))  
    s1 = 0  
    s2 = 0  
    for j in range(0, len(length),3):  
        a = length[j]  
        b = length[j+1]  
        c = length[j+2]  
        if a+b>c and a+c>b and b+c>a:  
            s1 += 1  
        else:  
            s2 += 1  
              
    print(s1,",", s2,sep="")  
